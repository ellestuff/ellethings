package ellestuff.ellethings.blocks;

import net.minecraft.block.TransparentBlock;

public class GelBlock extends TransparentBlock {
    public GelBlock(Settings settings) {
        super(settings);
    }
}
